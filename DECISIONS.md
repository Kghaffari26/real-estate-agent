# Autonomous session decisions

One line per consequential judgment call made while working unattended.

- Kghaffari26/agents-core has no commit (any branch) with `src/agents_core/{guards,registry}.py`: it's actually a monorepo named "agents-hub" internally (`core/*` namespace, class-based `Agent`/`fetch`/`transform`/`analyze` contract, `[tool.uv] package = false`, not pip-installable), not the installable `agents_core` package the instructions describe. Treating this as "no such commit exists yet" per the instructions' own fallback: doing step 3 (network-independent) now, then polling for the right commit periodically (ScheduleWakeup, not blocking sleep, since a single 15-min sleep exceeds this tool's max timeout) for up to ~4 hours before giving up and documenting the blocker in STATUS.md.
- Confirmed (read core/agent.py, core/registry.py, .github/workflows/run-agent.yml from agents-core@main): it's not just a path/naming mismatch — agents-core is architecturally a monorepo expecting each agent's code physically inside it (agents/<id>/agent.py exposing a core.agent.Agent subclass named AGENT, loaded by core.registry.load() via importlib), with its own run-agent.yml checking out the CALLING repo and running `python -m core.runner <agent>`. This is incompatible with "this repo holds only the real estate agent" as a standalone pip-consumer. Deleted the local .github/workflows/run-agent.yml (genuinely superseded — agent-real-estate.yml now calls agents-core's real one) but keeping the local agents-core/ package for now: deleting it with nothing compatible to replace it would break every test and the whole pipeline, contradicting "keep all existing tests passing." Treating this as the "work around it minimally" case the instructions explicitly allow when agents-core lacks something needed. agent-real-estate.yml is written and wired but will not actually succeed a run yet (documented in STATUS.md); it also omits max_run_usd/site_repo/cache_path since agents-core's run-agent.yml doesn't accept them.
